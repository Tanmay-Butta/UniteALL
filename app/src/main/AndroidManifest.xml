<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" /> <!-- For OSM tile caching -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo"
        android:label="UniteALL"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._4all"
        tools:targetApi="31">

        <!-- Optional legacy support -->
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <!-- SplashActivity (entry point) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".SignInActivity"
            android:exported="true"
            android:label="@string/app_name" />

        <!-- User Profile -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- Post Event -->
        <activity
            android:name=".ui.events.PostEventActivity"
            android:exported="false" />

        <!-- Event Details -->
        <activity
            android:name=".ui.events.EventDetailsActivity"
            android:exported="true" />

        <!-- If you had another DetailsActivity (check duplication) -->
        <activity
            android:name=".DetailsActivity"
            android:exported="false" />

        <!-- Location Activity -->
        <activity
            android:name=".LocationActivity"
            android:exported="false" />

        <!-- Main Activity (hosts fragments like HomeFragment) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" />

    </application>
</manifest>
